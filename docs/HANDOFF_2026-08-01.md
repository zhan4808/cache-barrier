# HANDOFF — instance shutdown state, 2026-08-01 (night)

**Everything of value is on NFS.** The working repo IS the NFS copy
(`cache-barrier-project/repos/cache-barrier`, HEAD `40d6de5`-ish, local
commits only — no push key on these boxes; remote github.com/zhan4808/cache-barrier).

## What this instance produced (2026-08-01, four sessions)

- **P0–P6 all executed.** Gate figure (`profiling/gate/`), reframe, portable
  harness + A100 transfer (`profiling/portable/`), dispatch cost model
  (`profiling/dispatch/`), paper body surgery (18 pp, compiles), 18-slide deck
  (`docs/presentation_2026-08-01_gate.html`, also a claude.ai artifact).
- **Served A/B complete** (`profiling/served/results_*`): decode fp8 1.447×,
  prefill 1.20×, ΔPPL +0.032, layer rel-err at gaussian floor, band demo
  closed negative. Full ledger: `RESULTS_2026-07.md`; work log:
  `JOURNAL_2026-08.md` (in-repo, committed).

## Lost with this instance (all recoverable, ~15 min + downloads)

- `~/models/Qwen3.6-27B` (52 GB; snapshot_download, see HANDOFF_2026-07-02 §0)
- `~/vllm-env` — restore via `env/vllm-0.20.2/restore.sh`, which now ALSO
  installs the deps this instance found missing (ninja, pandas, pyarrow,
  matplotlib). Serving additionally needs `VLLM_USE_DEEP_GEMM=0` — already
  exported inside `profiling/served/run_all.sh`.
- apt: texlive-latex-extra + texlive-fonts-recommended + texlive-bibtex-extra
  (paper build), poppler-utils, ninja-build, nsight-compute (if NCU needed)
- pip3 --user: playwright (+ `python3 -m playwright install chromium`) — only
  for deck rendering verification
- GPU clocks were reset (`-rgc`) and GPU memory is 0 MiB.

## Next session: A100

The kickoff prompt is committed at `repos/cache-barrier/KICKOFF_A100.md`.
Primary goal: run `profiling/portable/measure_params.py` on the A100, replace
the estimated constants in `transfer_validation.py`, re-report transfer MAPE.
Stretch: the gate flip on A100 (reduced Figure-1 sweep against measured
A100 C_eff).

## Open threads after A100

- One non-NVIDIA backend for the harness (FlagOS pitch)
- B200: single prediction — ceiling term vanishes with native-FP4 MMA
  (`cuda_validation/B200_RUNBOOK.md`; needs fresh Blackwell vLLM build)
- Paper case-study sections could compress further; push to GitHub from a
  box with a key
- Band-aware prefill batch shaping (mechanism B) — unexercised by the decode
  A/B; would need a prefill-heavy served workload
