# HANDOFF — state after the 2026-08-02 B200 session (session 8)

**Everything committed through `9fb3f06`** and synced back to the canonical
H100-region NFS (`ubuntu@68.209.75.33:~/robert-nfs/`). GitHub push still
pending a fresh PAT (the old one was revoked per the previous handoff) — the
canonical NFS and this box both carry the full repo.

## What this session established (JOURNAL_2026-08.md session 8, both parts)

- **Third architecture point**: B200 C_eff **98.8 MB = 0.781× nominal**;
  the effective/nominal ratio is 0.780/0.780/0.781 on A100/H100/B200.
- **Transfer simplifies on B200**: constants-only 17.6/1.8 (below/above);
  final **16.6/3.9 zero-shot with a ZERO-PARAMETER band** (keep C_hi/C_eff
  = 1.56 from H100, ramp to bw_hbm, no floor, no baseline term). The
  H100-fitted persistent floor is REFUTED on B200; two-point kernel terms
  require locally-smooth target latency, which sm_100 kernel selection
  violates (56 MB cell 2× its neighbors).
- **The native-FP4 prediction CONFIRMED** (the thesis-completer): W4A4
  NVFP4 cutlass never crosses bf16 (2.1–3.1×, T=16…2048); Marlin W4A16
  crosses at T*≈159, loses 2.4× at T=2048; the two FP4 paths sit **6.3×
  apart at T=2048** = the in-core dequant ceiling, removed by native MMA.
  Correctness decomposed: 0.2212 rel-err = intrinsic fp4 act-quant noise
  (software emulation: 0.2221). carm b200 block now MEASURED
  (native_peak_mult fp4 = 2.6 measured vs 4.0 projected).
- Deliverables: params_nvidia-b200.json, gate sweep + extension, transfer
  JSONs + fig, results_cuda_moe_b200 / results_moe_nvfp4_native_b200 /
  results_flashmla_sparse_b200 / carm_cuda_params_b200, money figure
  (figures/nvfp4_ceiling_break_h100_vs_b200), REPORT addendum, paper
  16 pp 0 errors, deck slide 12 + status updated, WeChat addendum (item 14).

## B200 box state (lost if it dies; all documented-recoverable)

- `~/vllm-b200-env` — vLLM 0.26.0 + torch 2.11 cu130; snapshot at
  `env/vllm-b200-0.26.0/` (freeze + restore.sh with SM100 assert).
- `/root/robert-nfs/` — full synced copy (regime-router excluded), repo at
  `9fb3f06`, identical to canonical.
- apt: texlive-latex-extra, texlive-fonts-recommended, texlive-bibtex-extra.
- **Clock lock UNAVAILABLE on this instance** (virtualized tier denies
  `-lgc` even with sudo; `-ac` deprecated). Sustained-load SM band measured
  1237–1320 MHz ±3%; noted in every B200 results file. Nothing to `-rgc`.

## Open, in order

> **Session-9 update (2026-08-02, H100 box)**: item 1 DONE (pushed with a
> PAT from the B200 box; auth since moved to an NFS-resident SSH key —
> `github-robert-ed25519`, wired into all repos). Item 3 DONE
> (`refit_floorfree_band.py`: floor worth ~3 pts below-gate locally, nothing
> above-gate; zero-param band is the transferable form). Item 4 prepped
> (`B200_RUNBOOK.md` §6). Also: the 0.780/0.780/0.781 ratio claim was
> grid-quantized — see JOURNAL session 9; honest claim ≈0.8× (H100 fine-grid
> 0.795±0.010).

1. ~~**GitHub push**~~ DONE — see session-9 note above.
2. **Non-NVIDIA backend** (FlagOS leg) — harness-only, ~1 h; goal-1
   methodology applies unchanged.
3. **Refit H100 with the floor-free band** — B200 says the floor is not a
   transferable element; check what H100's above-gate branch pays (the
   11.6→13.3 question from session 7, now with a third data point).
4. Tuned-CUDA w8a8 gate sweep on B200 (the triton 3.4/sm_100 confound —
   direction already resolved by the Marlin curves, magnitude open).
5. Wave/tile attribution of the prefill GEMM sawtooth.
6. Paper: related-work refresh + venue formatting pass.
