# HANDOFF — state after the 2026-08-01/02 A100 + H100 sessions

**Everything of value is committed and pushed.** Repo:
`cache-barrier-project/repos/cache-barrier`, HEAD `29e9bdd`+ (KICKOFF_B200
lands after), pushed to github.com/zhan4808/cache-barrier (main). The PAT used
for that push was shared in a chat session — **revoke it** and mint a fresh
one when the next box needs to push.

## Filesystem topology (read before touching either copy)

- **Canonical**: the H100-region NFS `/lambda/nfs/robert-nfs/` (this tree).
- **Mirror**: `/lambda/nfs/robert-nfs-west-2/` (west region; created for the
  A100 leg). Fully mirrored through commit `10fd88e` + the 70 GB env/model
  trees; **behind by the session-7 commits and final docs** (the A100
  instance died mid-final-sync). §sync below. GitHub now also carries the
  full repo, so the freshest repo is always a clone away.
- Rule that avoided disaster twice: one region is canonical per project at a
  time; sync directionally; never blind two-way rsync (and exclude an
  actively-worked repo from bulk mirror jobs — a bulk rsync mid-session will
  clobber remote work with stale files).

## What these sessions established (see JOURNAL_2026-08.md sessions 5–7)

- A100 constants measured (C_eff 31.2 MB = 0.78× nominal — same ratio as
  H100); transfer target re-measured graph-timed on the same GPU.
- **Residency is footprint-gated** (W+act+out, not the weight operand), soft
  collapse; confirmed by timing (`results_footprint_gate.json`) and NCU
  warm-state counters (`results_ncu_footprint.json`).
- Both below-gate residuals closed: A100's was the sm_80 baseline kernel
  (wmma/gemvx at M=1; two-point calibration), H100's was the soft band
  (fitted C_hi=56 MB, floor 2.10 TB/s, transfers normalized). Final A100
  zero-shot fp16: **12.4% below / 13.2% above gate** (held-out).
- Mechanism B bridged kernel↔engine: served chunk caps 896→1024 step +24%
  (reproduced), matching a +17% per-token sawtooth in the model's own prefill
  GEMM shapes; **no cap beats uncapped** — shaping avoids losses only.
  mb=2048 hangs vLLM 0.20.2 + hybrid GDN + chunked prefill outright.
- Paper: case study compressed, both residuals + bridge written in; **16 pp,
  0 errors**. Deck (17 slides) fully current; artifact same URL.
- Env lessons burned into `env/vllm-0.20.2/restore.sh`: extras install on
  every path now; GDN JIT needs **apt** ninja; kill leaked EngineCore pids
  via `nvidia-smi --query-compute-apps` before relaunching.

## Local-disk state of the 2026-08-02 H100 instance (lost if it dies)

`~/vllm-env` (restore.sh), `~/models/Qwen3.6-27B` (52 GB, snapshot_download),
apt: texlive-latex-extra/fonts-recommended/bibtex-extra, ninja-build,
nsight-compute. All documented-recoverable.

## Next session: B200 (the two remaining predictions)

Kickoff prompt committed at `KICKOFF_B200.md` (repo root). Goals in order:
(1) third architecture point — harness + reduced gate sweep + transfer on
Blackwell's ~126 MB LLC; (2) the native-FP4 ceiling-vanishes prediction —
full plan in `profiling/cuda_validation/B200_RUNBOOK.md`. If a Blackwell box
is not available first, the FlagOS/non-NVIDIA leg (any torch-plugin
accelerator, harness-only, ~1 h) is the alternative; goal-1 methodology
applies unchanged.

## §sync — settling the west-2 debt

On the next west-region instance (or from any box that can reach both):
```bash
rsync -az <canonical>/cache-barrier-project/repos/ <west2>/cache-barrier-project/repos/
rsync -az <canonical>/cache-barrier-project/docs/  <west2>/cache-barrier-project/docs/
```
or simply `git pull` from GitHub inside the west-2 repo checkout, then copy
docs. Check `git log --oneline -1` on both sides afterward.

## Open after B200

- FlagOS/non-NVIDIA backend (if not done instead of B200)
- Wave/tile attribution of the prefill GEMM sawtooth (nvjet tile configs)
- Above-gate cost of the transition floor on H100 (11.6→13.3% — is a
  regime-dependent floor justified?)
- Paper: related-work refresh + venue formatting pass
